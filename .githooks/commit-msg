#!/bin/sh
# Reject Co-authored-by trailers (local hook — enable: git config core.hooksPath .githooks)
grep -qiE '^Co-authored-by:' "$1" && {
  echo "commit-msg: rejected — Co-authored-by trailers are not allowed in this repo" >&2
  exit 1
}
exit 0
