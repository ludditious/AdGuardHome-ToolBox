# Optional — not required to run the container.
# Secrets are auto-generated on first start and saved in the /data volume.
#
# SECRET_KEY=override-only-if-you-need-a-fixed-key
# CRON_SECRET=override-only-if-you-need-a-fixed-key
# ALLOW_REGISTRATION=false
